import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  serverExternalPackages: ['mammoth', 'docx', 'docxtemplater', 'pizzip', 'docxtemplater-image-module-free'],
};

export default nextConfig;
